# 构建产物
build/

# 编译中间文件
*.o

# 编辑器 / 工具
.vscode/
.cache/
.DS_Store
compile_commands.json

# ROS2 构建与日志（colcon / ros2 运行）
install/
log/
.roslog/

# 运行输出：results/ 下的 *.avi / *.png 均为程序生成物，不入库
results/*
# 例外：真实相机验证录屏是提交证据，需入库（README 引用此文件）
!results/real_camera_2026-09-09.mkv
# 例外：检测器 A/B 评测的汇总 —— 本仓库作者自己跑出来的原始数据（约 1 KB，供直接核对数字）；
#       有顾虑可照 README「题2 → 快速验证」第 3 条的 eval_demo 命令自己重跑一遍复现
!results/eval_baseline_summary.txt
!results/eval_pose_summary.txt

# 海康 MVS SDK 运行日志（hik_probe 运行后自动生成）
MvSdkLog/
